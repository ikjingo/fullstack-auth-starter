# 개발 환경 전용 설정
# 프로덕션에서는 절대 사용하지 않음

spring:
  profiles:
    active: dev

jwt:
  # 개발 환경 전용 시크릿 - 프로덕션에서 사용 금지!
  secret: dev-only-secret-key-for-local-development-must-be-at-least-256-bits

# 개발환경에서는 Admin IP 화이트리스트 비활성화
admin:
  ip-whitelist:
    enabled: false
