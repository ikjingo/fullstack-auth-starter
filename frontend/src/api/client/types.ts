/**
 * API 클라이언트 타입 정의
 *
 * @deprecated '@/types'에서 직접 import 권장
 * 이 파일은 하위 호환성을 위해 유지됩니다.
 */

export {
  type ApiResponseWrapper,
  type QueryParamValue,
  type QueryParams,
  type RequestConfig,
  type ApiErrorLegacy,
  type PaginationParams,
  type PageResponse,
} from '@/types/api'
