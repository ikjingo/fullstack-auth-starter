/**
 * 스타일 유틸리티 Re-export
 *
 * @deprecated 이 파일은 하위 호환성을 위해 유지됩니다.
 * 새 코드에서는 '@/utils/styles'에서 직접 import하세요.
 *
 * @example
 * // 기존 (하위 호환)
 * import { pageContainer, card } from '@/utils'
 *
 * // 권장 (새 구조)
 * import { pageContainer, card } from '@/utils/styles'
 * // 또는 카테고리별
 * import { pageContainer, card } from '@/utils/styles/layout'
 */

// 모든 스타일 유틸리티를 새 모듈에서 re-export
export * from './styles'
