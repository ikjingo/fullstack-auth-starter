/**
 * 플렉스 레이아웃 관련 스타일 유틸리티
 */

// ============================================================================
// 기본 플렉스 정렬
// ============================================================================

/** 플렉스 중앙 정렬 */
export const flexCenter = 'flex items-center justify-center'

/** 플렉스 양쪽 정렬 */
export const flexBetween = 'flex items-center justify-between'

// ============================================================================
// 플렉스 행 + 갭
// ============================================================================

/** 플렉스 행 + 갭 - 작은 (gap-2) */
export const flexRowGapSm = 'flex items-center gap-2'

/** 플렉스 행 + 갭 - 기본 (gap-3) */
export const flexRowGap = 'flex items-center gap-3'

/** 플렉스 행 + 갭 - 큰 (gap-4) */
export const flexRowGapLg = 'flex items-center gap-4'

// ============================================================================
// 플렉스 컬럼
// ============================================================================

/** 플렉스 컬럼 + 센터 */
export const flexColCenter = 'flex flex-col items-center justify-center'

/** 플렉스 컬럼 + 갭 - 작은 (gap-2) */
export const flexColGapSm = 'flex flex-col gap-2'

/** 플렉스 컬럼 + 갭 - 기본 (gap-3) */
export const flexColGap = 'flex flex-col gap-3'

/** 플렉스 컬럼 + 갭 - 큰 (gap-4) */
export const flexColGapLg = 'flex flex-col gap-4'

// ============================================================================
// 빈 상태/로딩 센터
// ============================================================================

/** 빈 상태/로딩 센터 레이아웃 */
export const emptyCenter = 'flex flex-col items-center justify-center py-12 text-center'
